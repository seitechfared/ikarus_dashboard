version: '3.9'

services:
  db:
    image: postgres:15
    restart: unless-stopped
    environment:
      POSTGRES_DB: ikarus
      POSTGRES_USER: ikarus
      POSTGRES_PASSWORD: ikarus
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ikarus"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
